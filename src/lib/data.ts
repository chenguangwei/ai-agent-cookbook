/**
 * Re-export categories from the shared config.
 * Kept for backward compatibility — prefer importing from '@/lib/categories' directly.
 */
export { TUTORIAL_CATEGORIES as CATEGORIES } from './categories';
export type { CategoryDef as Category } from './categories';
